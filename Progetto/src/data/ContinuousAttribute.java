package data;

public class ContinuousAttribute extends Attribute {

    /* Costruisce un attributo continuo (target numerico o variabile continua). */
    public ContinuousAttribute(String name, int index) {
        super(name, index);
    }
}
